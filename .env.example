# Backend API base URL (no trailing slash; axios will add slash when needed)
REACT_APP_BACKEND_URL=http://localhost:8000

# Optional: Production API (use .env.local to override locally)
# REACT_APP_BACKEND_URL=https://api.securemyair.com
